export type Event = unknown;

export async function readAllEvents(baseUrl: string): Promise<Event[]> {
    // TODO: implement — consume the SSE stream, return array of parsed JSON events
    return [];
}
